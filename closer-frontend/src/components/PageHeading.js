import React from 'react'

const PageHeading = ({ text }) => {
    return (
        <div className='page-heading big-header'>
            <h1>{text}</h1>
        </div>
    )
}

export default PageHeading